# Trigger Test Prompts

## artifacts-builder
- Help me use `artifacts-builder` on a realistic work artifact.
- Should `artifacts-builder` trigger for this request? Explain why before acting.
- Do not use `artifacts-builder`; handle this nearby but different request normally.

## web-artifacts-builder
- Help me use `web-artifacts-builder` on a realistic work artifact.
- Should `web-artifacts-builder` trigger for this request? Explain why before acting.
- Do not use `web-artifacts-builder`; handle this nearby but different request normally.

## canvas-design
- Help me use `canvas-design` on a realistic work artifact.
- Should `canvas-design` trigger for this request? Explain why before acting.
- Do not use `canvas-design`; handle this nearby but different request normally.

## theme-factory
- Help me use `theme-factory` on a realistic work artifact.
- Should `theme-factory` trigger for this request? Explain why before acting.
- Do not use `theme-factory`; handle this nearby but different request normally.

## algorithmic-art
- Help me use `algorithmic-art` on a realistic work artifact.
- Should `algorithmic-art` trigger for this request? Explain why before acting.
- Do not use `algorithmic-art`; handle this nearby but different request normally.

## slack-gif-creator
- Help me use `slack-gif-creator` on a realistic work artifact.
- Should `slack-gif-creator` trigger for this request? Explain why before acting.
- Do not use `slack-gif-creator`; handle this nearby but different request normally.

## visual-design-foundations
- Help me use `visual-design-foundations` on a realistic work artifact.
- Should `visual-design-foundations` trigger for this request? Explain why before acting.
- Do not use `visual-design-foundations`; handle this nearby but different request normally.

## image-enhancer
- Help me use `image-enhancer` on a realistic work artifact.
- Should `image-enhancer` trigger for this request? Explain why before acting.
- Do not use `image-enhancer`; handle this nearby but different request normally.
