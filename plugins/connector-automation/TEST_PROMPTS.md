# Trigger Test Prompts

## figma-automation
- Help me use `figma-automation` on a realistic work artifact.
- Should `figma-automation` trigger for this request? Explain why before acting.
- Do not use `figma-automation`; handle this nearby but different request normally.

## miro-automation
- Help me use `miro-automation` on a realistic work artifact.
- Should `miro-automation` trigger for this request? Explain why before acting.
- Do not use `miro-automation`; handle this nearby but different request normally.

## notion-automation
- Help me use `notion-automation` on a realistic work artifact.
- Should `notion-automation` trigger for this request? Explain why before acting.
- Do not use `notion-automation`; handle this nearby but different request normally.

## mcp-builder
- Help me use `mcp-builder` on a realistic work artifact.
- Should `mcp-builder` trigger for this request? Explain why before acting.
- Do not use `mcp-builder`; handle this nearby but different request normally.
