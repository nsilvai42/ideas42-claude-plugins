# Trigger Test Prompts

## deep-research
- Help me use `deep-research` on a realistic work artifact.
- Should `deep-research` trigger for this request? Explain why before acting.
- Do not use `deep-research`; handle this nearby but different request normally.

## article-extractor
- Help me use `article-extractor` on a realistic work artifact.
- Should `article-extractor` trigger for this request? Explain why before acting.
- Do not use `article-extractor`; handle this nearby but different request normally.

## research-writer
- Help me use `research-writer` on a realistic work artifact.
- Should `research-writer` trigger for this request? Explain why before acting.
- Do not use `research-writer`; handle this nearby but different request normally.

## concept-map
- Help me use `concept-map` on a realistic work artifact.
- Should `concept-map` trigger for this request? Explain why before acting.
- Do not use `concept-map`; handle this nearby but different request normally.

## agent-council
- Help me use `agent-council` on a realistic work artifact.
- Should `agent-council` trigger for this request? Explain why before acting.
- Do not use `agent-council`; handle this nearby but different request normally.

## journey-mapping
- Help me use `journey-mapping` on a realistic work artifact.
- Should `journey-mapping` trigger for this request? Explain why before acting.
- Do not use `journey-mapping`; handle this nearby but different request normally.
