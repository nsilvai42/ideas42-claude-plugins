# Trigger Test Prompts

## doc-coauthoring
- Help me use `doc-coauthoring` on a realistic work artifact.
- Should `doc-coauthoring` trigger for this request? Explain why before acting.
- Do not use `doc-coauthoring`; handle this nearby but different request normally.

## word-documents
- Help me use `word-documents` on a realistic work artifact.
- Should `word-documents` trigger for this request? Explain why before acting.
- Do not use `word-documents`; handle this nearby but different request normally.

## pdf-documents
- Help me use `pdf-documents` on a realistic work artifact.
- Should `pdf-documents` trigger for this request? Explain why before acting.
- Do not use `pdf-documents`; handle this nearby but different request normally.

## powerpoint-decks
- Help me use `powerpoint-decks` on a realistic work artifact.
- Should `powerpoint-decks` trigger for this request? Explain why before acting.
- Do not use `powerpoint-decks`; handle this nearby but different request normally.

## excel-workbooks
- Help me use `excel-workbooks` on a realistic work artifact.
- Should `excel-workbooks` trigger for this request? Explain why before acting.
- Do not use `excel-workbooks`; handle this nearby but different request normally.

## theme-factory
- Help me use `theme-factory` on a realistic work artifact.
- Should `theme-factory` trigger for this request? Explain why before acting.
- Do not use `theme-factory`; handle this nearby but different request normally.
