# Trigger Test Prompts

## figma-implement-design
- Help me use `figma-implement-design` on a realistic work artifact.
- Should `figma-implement-design` trigger for this request? Explain why before acting.
- Do not use `figma-implement-design`; handle this nearby but different request normally.

## figma-code-connect
- Help me use `figma-code-connect` on a realistic work artifact.
- Should `figma-code-connect` trigger for this request? Explain why before acting.
- Do not use `figma-code-connect`; handle this nearby but different request normally.

## figma-design-system-rules
- Help me use `figma-design-system-rules` on a realistic work artifact.
- Should `figma-design-system-rules` trigger for this request? Explain why before acting.
- Do not use `figma-design-system-rules`; handle this nearby but different request normally.

## react-performance
- Help me use `react-performance` on a realistic work artifact.
- Should `react-performance` trigger for this request? Explain why before acting.
- Do not use `react-performance`; handle this nearby but different request normally.

## web-ui-review
- Help me use `web-ui-review` on a realistic work artifact.
- Should `web-ui-review` trigger for this request? Explain why before acting.
- Do not use `web-ui-review`; handle this nearby but different request normally.

## visual-design-foundations
- Help me use `visual-design-foundations` on a realistic work artifact.
- Should `visual-design-foundations` trigger for this request? Explain why before acting.
- Do not use `visual-design-foundations`; handle this nearby but different request normally.

## image-enhancer
- Help me use `image-enhancer` on a realistic work artifact.
- Should `image-enhancer` trigger for this request? Explain why before acting.
- Do not use `image-enhancer`; handle this nearby but different request normally.

## ui-ux-design-review
- Help me use `ui-ux-design-review` on a realistic work artifact.
- Should `ui-ux-design-review` trigger for this request? Explain why before acting.
- Do not use `ui-ux-design-review`; handle this nearby but different request normally.
