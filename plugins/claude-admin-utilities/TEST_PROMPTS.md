# Trigger Test Prompts

## memory-consolidation
- Help me use `memory-consolidation` on a realistic work artifact.
- Should `memory-consolidation` trigger for this request? Explain why before acting.
- Do not use `memory-consolidation`; handle this nearby but different request normally.

## setup-cowork
- Help me use `setup-cowork` on a realistic work artifact.
- Should `setup-cowork` trigger for this request? Explain why before acting.
- Do not use `setup-cowork`; handle this nearby but different request normally.

## schedule
- Help me use `schedule` on a realistic work artifact.
- Should `schedule` trigger for this request? Explain why before acting.
- Do not use `schedule`; handle this nearby but different request normally.

## create-plugin
- Help me use `create-plugin` on a realistic work artifact.
- Should `create-plugin` trigger for this request? Explain why before acting.
- Do not use `create-plugin`; handle this nearby but different request normally.

## skill-lookup
- Help me use `skill-lookup` on a realistic work artifact.
- Should `skill-lookup` trigger for this request? Explain why before acting.
- Do not use `skill-lookup`; handle this nearby but different request normally.
