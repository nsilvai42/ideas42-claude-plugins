# Trigger Test Prompts

## memory-import
- Help me use `memory-import` on a realistic work artifact.
- Should `memory-import` trigger for this request? Explain why before acting.
- Do not use `memory-import`; handle this nearby but different request normally.

## project-setup
- Help me use `project-setup` on a realistic work artifact.
- Should `project-setup` trigger for this request? Explain why before acting.
- Do not use `project-setup`; handle this nearby but different request normally.

## file-organizer
- Help me use `file-organizer` on a realistic work artifact.
- Should `file-organizer` trigger for this request? Explain why before acting.
- Do not use `file-organizer`; handle this nearby but different request normally.

## create-plugin
- Help me use `create-plugin` on a realistic work artifact.
- Should `create-plugin` trigger for this request? Explain why before acting.
- Do not use `create-plugin`; handle this nearby but different request normally.

## skill-lookup
- Help me use `skill-lookup` on a realistic work artifact.
- Should `skill-lookup` trigger for this request? Explain why before acting.
- Do not use `skill-lookup`; handle this nearby but different request normally.

## review-console
- Help me use `review-console` on a realistic work artifact.
- Should `review-console` trigger for this request? Explain why before acting.
- Do not use `review-console`; handle this nearby but different request normally.

## review-council
- Help me use `review-council` on a realistic work artifact.
- Should `review-council` trigger for this request? Explain why before acting.
- Do not use `review-council`; handle this nearby but different request normally.
